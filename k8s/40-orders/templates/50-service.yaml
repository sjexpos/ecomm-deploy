apiVersion: v1
kind: Service
metadata:
  labels:
    app: orders-service
    type: spring-boot
  name: orders-service
  namespace: {{ .Release.Namespace }}
spec:
  ports:
    - name: http-traffic
      port: 8000
      protocol: TCP
      targetPort: 7000
  selector:
    app: orders-service
