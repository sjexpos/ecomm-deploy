apiVersion: v1
kind: Service
metadata:
  labels:
    app: limiter-kafka-mps
    type: spring-boot
  name: limiter-kafka-mps
  namespace: {{ .Release.Namespace }}
spec:
  ports:
    - name: http
      port: 8000
      protocol: TCP
      targetPort: 7000
  selector:
    app: limiter-kafka-mps
