apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: limiter-kafka-mps-monitor
  labels:
    app: limiter-kafka-mps-monitor
    release: prometheus-operator
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    matchLabels:
      app: limiter-kafka-mps
  namespaceSelector:
    matchNames:
      - {{ .Release.Namespace }}
  endpoints:
    - port: http-traffic
      path: "/actuator/prometheus"
      interval: 5s
      scrapeTimeout: 5s
