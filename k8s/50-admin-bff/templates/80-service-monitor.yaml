apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: admin-bff-monitor
  labels:
    app: admin-bff-monitor
    release: prometheus-operator
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    matchLabels:
      app: admin-bff
  namespaceSelector:
    matchNames:
      - {{ .Release.Namespace }}
  endpoints:
    - port: http-traffic
      path: "/actuator/prometheus"
      interval: 5s
      scrapeTimeout: 5s
