apiVersion: v1
kind: Service
metadata:
  labels:
    app: users-service
    type: spring-boot
  name: users-service
  namespace: {{ .Release.Namespace }}
spec:
  ports:
    - name: http-traffic
      port: 8000
      protocol: TCP
      targetPort: 7000
  selector:
    app: users-service
